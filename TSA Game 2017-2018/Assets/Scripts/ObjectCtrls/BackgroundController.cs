using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundController : MonoBehaviour {

    public bool blockSpriteRandomiser; //If this background's sprite should be randomised during runtime; If true, will STOP the randomisation
}
